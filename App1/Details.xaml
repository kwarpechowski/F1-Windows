<Page
    x:Class="App1.Details"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition />
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Uid="Details" VerticalAlignment="Top" Style="{StaticResource Header}"/>
        <TextBlock Grid.Row="1" x:Name="TitleHeader" VerticalAlignment="Top" />
        <TextBlock Grid.Row="2" x:Name="Status" VerticalAlignment="Top" />

        <Grid Grid.Row="3" x:Name="Grid">
            
        </Grid>
        <Button Grid.Row="4" Content="back" HorizontalAlignment="Stretch" Height="30" />
    </Grid>
</Page>
